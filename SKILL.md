---
name: formula-extract
description: 从 PDF 或 DOCX 文献中提取公式，由当前大模型将公式转换为 LaTeX 代码，并保存到 Word 文档中。当用户提出“提取公式”“公式转 LaTeX”“提取文献中的公式代码”等请求时使用。
---

# 文献公式提取

将一个或多个 PDF、DOCX 文件中的公式转换为 LaTeX 代码，并将结果保存为 Word 文档。

本 Skill 不调用 Mathpix 或其他外部公式识别 API。公式识别与 LaTeX 转写由当前大模型完成；本地程序仅负责读取文件、筛选公式候选、提取文本与坐标、必要时截取局部公式区域，以及生成 Word 文档。

## 核心原则

1. 对文字型 PDF 优先读取文本层，不默认逐页渲染为图片。
2. 先使用本地程序筛选疑似公式，再将候选公式交给大模型。
3. 结构完整的简单公式直接根据文本层转为 LaTeX。
4. 仅对结构破损或复杂公式截取局部区域进行视觉识别。
5. 不将无公式页面交给大模型。
6. 不为每一页或每一个简单公式单独发起模型识别。
7. 无法可靠识别的公式标记为“需人工核验”，不得猜测补全。

## 支持文件

仅处理以下格式：

- `.pdf`
- `.docx`

已有源文件或文件路径时，不再询问用户重复提供。

## 总体流程

```text
读取源文件
→ 判断文件类型
→ 提取文本层、排版信息和公式候选
→ 跳过无公式内容
→ 简单公式直接转写
→ 复杂公式局部截图识别
→ 分批生成 LaTeX
→ 按原文顺序写入 Word
```

## PDF 处理流程

### 第一阶段：判断 PDF 类型

1. 检查 PDF 是否存在可读取的文本层。
2. 根据检测结果自动分流：
   - 有可用文本层：执行“文字型 PDF 流程”。
   - 无可用文本层：执行“扫描型 PDF 流程”。
3. 不询问用户选择处理方式。

### 第二阶段：文字型 PDF 快速扫描

1. 使用本地程序一次性读取以下信息：
   - 每页文本；
   - 文本块；
   - 字符坐标；
   - 字体与字号；
   - 图片和矢量图形位置；
   - 页面尺寸。
2. 此阶段不调用大模型。
3. 此阶段不生成整页图片。
4. 根据以下特征筛选疑似公式页面和公式文本块：
   - 包含 `=、≠、≈、≤、≥、±、×、÷、∑、∫、√、∞` 等数学符号；
   - 包含希腊字母或常见数学变量；
   - 存在类似 `(1)`、`（2）`、`[3]` 的独立公式编号；
   - 文本块明显居中、缩进异常或与正文排版不同；
   - 一行中变量、数字和运算符占比较高；
   - 存在上下标、小字号字符或数学字体；
   - 存在分数线、根号、矩阵括号或多行对齐结构。
5. 没有公式特征的页面直接跳过，不交给大模型。

### 第三阶段：公式候选整理

1. 按公式在原文中的出现顺序提取候选文本块。
2. 根据坐标位置合并属于同一公式的相邻字符或文本块。
3. 去除以下无关内容：
   - 页眉；
   - 页脚；
   - 页码；
   - 普通正文段落；
   - 图表标题；
   - 单独的公式编号。
4. 每个候选公式记录：
   - 唯一编号；
   - 来源页码；
   - 页面坐标；
   - 提取文本；
   - 原公式编号；
   - 是否需要局部截图；
   - 初步置信度。

### 第四阶段：公式分流

#### A. 简单公式

满足以下条件时，不生成图片，直接根据文本层内容转写为 LaTeX：

- 单行公式；
- 字符顺序完整；
- 等号和运算符关系清楚；
- 上下标关系基本明确；
- 不包含复杂分式、矩阵、分段函数或多行推导。

处理方式：

```text
公式候选文本
→ 当前大模型整理数学结构
→ 输出标准 LaTeX
```

#### B. 复杂或结构破损公式

出现以下情况时，仅截取公式所在的局部区域：

- 上下标位置丢失或顺序混乱；
- 分子、分母被拆成多行；
- 根号作用范围无法确定；
- 积分、求和或极限的上下限位置不清；
- 矩阵、行列式、分段函数；
- 多行公式或对齐推导；
- 大括号覆盖范围不清；
- 字符提取结果明显不完整；
- 大模型仅根据文本无法可靠恢复。

局部截图要求：

1. 根据候选公式的页面坐标确定裁剪区域。
2. 仅渲染公式区域，不渲染整页。
3. 在公式四周保留少量空白，避免裁掉上下标或编号。
4. 不包含大段正文、页眉、页脚或无关图表。
5. 将提取文本与局部截图一起交给大模型。

### 第五阶段：扫描型 PDF

扫描型 PDF 无可用文本层时，执行以下流程：

1. 先以较低分辨率读取页面，用于筛选疑似公式区域。
2. 不直接对所有页面进行高分辨率渲染。
3. 优先根据以下特征定位公式：
   - 居中的独立行；
   - 公式编号；
   - 数学符号密集区域；
   - 明显的分式、根号、矩阵或大括号结构。
4. 仅对疑似公式区域进行高分辨率局部截图。
5. 将多个局部公式截图分批交给大模型。
6. 若无法可靠定位公式，可标记该页“需人工核验”，不得反复进行无差别整页识别。

## DOCX 处理流程

1. 优先读取 DOCX 中的 OMML 公式对象。
2. 将 OMML 公式转换或整理为标准 LaTeX。
3. 对以普通字符输入的公式，根据段落内容和排版识别公式候选。
4. 对以图片形式插入的公式，仅提取公式图片并交给当前大模型识别。
5. 不将整个 DOCX 页面渲染为图片。
6. 不提取公式前后的正文，除非用户明确要求。

## 大模型识别规则

### 输入规则

每批只提供：

- 公式唯一编号；
- 提取出的公式文本；
- 必要时提供公式局部截图；
- 必要的少量上下文。

禁止提供：

- 整篇文献；
- 整页无关正文；
- 无公式页面；
- 大段公式解释文字。

### 批量处理

1. 将多个候选公式组成一批处理。
2. 建议每批包含 10 至 20 个公式候选。
3. 不为每个公式单独发起一次识别。
4. 公式较复杂或图片较多时，可适当减少单批数量。
5. 每处理完一批，立即保存中间结果，支持断点续跑。

### 输出格式

要求大模型按唯一编号返回结构化结果：

```json
[
  {
    "id": "formula_001",
    "latex": "Y_{it}=\\alpha+\\beta X_{it}+\\varepsilon_{it}",
    "confidence": "high",
    "needs_review": false
  }
]
```

### 转写要求

1. 仅输出原文公式对应的 LaTeX。
2. 不解释公式含义。
3. 不补充原文中不存在的变量、条件、括号或符号。
4. 保留原公式中的：
   - 上标；
   - 下标；
   - 分数；
   - 根号；
   - 积分；
   - 求和；
   - 极限；
   - 矩阵；
   - 分段结构；
   - 多行对齐关系。
5. 公式编号与 LaTeX 主体分开保存。
6. 无法确定时：
   - `confidence` 设为 `low`；
   - `needs_review` 设为 `true`；
   - 不得猜测补全。

## 置信度规则

### high

- 文本结构完整；
- 符号关系清楚；
- 大模型能够可靠恢复；
- 不需要图片辅助。

### medium

- 文本存在少量顺序问题；
- 已结合局部截图完成识别；
- 主要结构明确，但仍建议抽查。

### low

- 字符缺失；
- 图片模糊；
- 结构复杂；
- 无法确定上下标、分式或符号范围；
- 需要人工核验。

## 输出要求

1. 按公式在原文中的出现顺序排列。
2. 每条结果默认仅包含：
   - 公式编号；
   - LaTeX 代码。
3. 对低置信度结果增加“需人工核验”标记。
4. 除非用户明确要求，否则不提取公式前后的正文内容。
5. 输出文件保存在源文件所在目录。
6. 输出文件命名为：

```text
<源文件名>_公式提取.docx
```

7. 完成后只返回输出文件，并附一句简短结果说明。

## 性能优化

### 跳过无关内容

1. 没有公式特征的页面直接跳过。
2. 默认跳过：
   - 封面；
   - 目录；
   - 致谢；
   - 参考文献；
   - 附录中的纯文字内容。
3. 检测到“参考文献”“References”“Bibliography”等标题后，若用户未要求处理后续内容，可停止继续扫描。
4. 不因个别页面识别失败而重新处理整篇文献。

### 缓存与断点续跑

1. 保存每页扫描结果和公式候选坐标。
2. 保存每批大模型识别结果。
3. 再次运行同一文件时，优先读取已有缓存。
4. 仅重新处理：
   - 尚未完成的公式；
   - 识别失败的公式；
   - 用户指定重新识别的公式。
5. 不重复渲染和识别已成功处理的内容。

### 去重

1. 对重复出现的公式进行文本和结构相似度检查。
2. 默认保留公式在原文中的每次出现。
3. 若用户明确要求去重，则只保留首次出现，并记录其他页码。

### 页码范围

1. 用户指定页码范围时，仅处理指定范围。
2. 用户未指定时，按全文快速扫描流程处理。
3. 不主动询问页码范围。

## 异常处理

### 依赖缺失

仅在程序明确提示缺少依赖时安装：

```bash
pip install python-docx pymupdf lxml pillow
```

这些依赖仅用于：

- 读取 PDF；
- 读取或生成 DOCX；
- 处理 XML；
- 裁剪局部图片。

它们不负责公式识别，也不需要 API 密钥。

### 文件异常

遇到以下情况时直接说明：

- 文件已加密；
- 文件已损坏；
- 文件格式不支持；
- 文件无可读取内容；
- 文献中未检测到公式。

不使用无关方式反复尝试。

### 识别异常

1. 单个公式识别失败时，只重试该公式，不重新处理整篇文献。
2. 最多进行一次局部重新提取或更高分辨率截图。
3. 仍无法确定时标记为“需人工核验”。
4. 不调用 Mathpix 或其他外部公式识别服务。

## 禁止事项

- 禁止默认逐页生成整页图片。
- 禁止将整篇 PDF 转换为图片后逐张识别。
- 禁止将没有公式的页面交给大模型。
- 禁止为每一页单独调用大模型。
- 禁止为每个简单公式单独调用大模型。
- 禁止调用 Mathpix。
- 禁止调用其他外部公式 OCR API。
- 禁止仅凭残缺文本猜测复杂公式。
- 禁止因单个公式失败而重新处理整篇文件。
- 禁止在最终结果中展示冗长的中间过程。
